extends layout

block content
  h1= title
  p I guess if you wanted, you could even post a file from curl or something.
  form(action='/files', method='post', enctype='multipart/form-data')
    each field in formFields
      br
      label.fieldLabel(for=field.name)= field.label
      input(id=field.name, type=field.type, name=field.name, placeholder=field.placeholder, value=field.value)
    br
    input(type='file', multiple, name='foofile')
    input(type='hidden', name="exif", id="exif")
    input(type='submit') Submit

  p Also, here's some files if you want to check them out.
  a(href='/files') The List

block footer
  script(src="/js/form.js")
